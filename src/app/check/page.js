
import BuyButton from "../components/BuyButton";

const page = () => {
    
    return (
        <div className="w-full h-screen mt-16 flex container items-center">
            
                    <BuyButton/>
        </div>
    );
};

export default page;